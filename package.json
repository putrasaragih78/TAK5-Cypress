{
  "name": "tak5-cypress",
  "version": "1.0.0",
  "description": "Cypress Automation",
  "main": "index.js",
  "scripts": {
    "test-product": "npx Cypress run --spec 'cypress/e2e/tricentis/product.cy.js' --browser chrome",
    "test-register": "npx Cypress run --spec 'cypress/e2e/tricentis/register.cy.js' --browser chrome",
    "test-tricentis": "npx Cypress run --spec 'cypress/e2e/tricentis/*' --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.10.0"
  }
}
